export default class Occupation {
    constructor(jour,StartH,StartMin,EndH,EndMin) {
      this.jour = jour
      this.StartH = StartH
      this.StartMin = StartMin
      this.EndH = EndH
      this.EndMin = EndMin
      
    }
  }