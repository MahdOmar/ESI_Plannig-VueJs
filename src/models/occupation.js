export default class Occupation {
    constructor(jour,type,StartH,StartMin,EndH,EndMin) {
      this.jour = jour
      this.type = type
      this.StartH = StartH
      this.StartMin = StartMin
      this.EndH = EndH
      this.EndMin = EndMin
      
    }
  }